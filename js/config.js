/* Front-end configuration.
   IMPORTANT: because this project has no server/database, staff credentials are
   visible to anyone who can inspect the browser source. Use this only for an
   internal/demo portal, or move authentication to a server later.
*/
window.PORTAL_CONFIG = {
  instituteName: "UN CAMPUS",
  portalTitle: "Results Portal",
  contactEmail: "info@uncampusacademy.com",
  staff: {
    username: "admin",
    password: "UN7983"
  },
  animation: {
    preloaderMinimumMs: 1200
  }
};
